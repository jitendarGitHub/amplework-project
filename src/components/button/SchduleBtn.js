import { Button } from "react-bootstrap";

const ScheduleBtn = () => {
  return (
    <>
      <Button
        color="warning"
        className="bg-warning text-dark px-4 py-2 fw-bold fs-5 mt-2"
      >
        Schedule a call
      </Button>
    </>
  );
};

export default ScheduleBtn;
